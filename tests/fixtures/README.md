# Integration fixtures

Integration tests may build deterministic source and destination trees beneath
this directory. The checked-in `empty-source` and `empty-destination`
directories provide stable roots for tests that start without any files.
